{
  "buildCommand": "npm run build --workspace=@gate/client",
  "outputDirectory": "apps/client/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
